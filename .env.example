# Copy to .env (git-ignored). Expo inlines EXPO_PUBLIC_* at build time.
EXPO_PUBLIC_SUPABASE_URL=https://vekgulejexranhdhecdp.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_fOPjo3c3G2zk6dsa-KKFjQ_fCYPmC25

# Strava OAuth app (strava.com/settings/api). The client id is public; the
# secret lives ONLY in Supabase edge-function secrets, never in the app.
EXPO_PUBLIC_STRAVA_CLIENT_ID=

# ---- Supabase edge-function secrets (set with `supabase secrets set …`) ----
# STRAVA_CLIENT_ID=
# STRAVA_CLIENT_SECRET=
# TOKEN_ENC_KEY=            # base64 of 32 random bytes: openssl rand -base64 32
# ANTHROPIC_API_KEY=
# COACH_MODEL=claude-sonnet-5
